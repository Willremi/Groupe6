{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}
{% block stylesheets %}
    
{% endblock %}
{% block body %}
    <h1>Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type.nom }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ type.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_index') }}"><button class="btn btn-outline-success">back to list<button></a>

    <a href="{{ path('type_edit', {'id': type.id}) }}"><button class="btn btn-outline-warning">edit</button></a>

    {{ include('admin/type/_delete_form.html.twig') }}
{% endblock %}
