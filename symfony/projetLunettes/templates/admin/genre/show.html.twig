{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}
{% block stylesheets %}
    
{% endblock %}
{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ genre.nomGenre }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ genre.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genre_index') }}"><button class="btn btn-outline-success">back to list</button></a>

    <a href="{{ path('genre_edit', {'id': genre.id}) }}"><button class="btn btn-outline-warning">edit</button></a>

    {{ include('admin/genre/_delete_form.html.twig') }}
{% endblock %}
