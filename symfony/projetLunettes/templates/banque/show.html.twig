{% extends 'base.html.twig' %}

{% block title %}Couleur{% endblock %}
{% block stylesheets %}
    
{% endblock %}
{% block body %}
    <h1>Couleur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ couleur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ couleur.nom }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ couleur.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('couleur_index') }}"><button class="btn btn-outline-success">back to list</button></a>

    <a href="{{ path('couleur_edit', {'id': couleur.id}) }}"><button class="btn btn-outline-warning">edit</button></a>

    {{ include('admin/couleur/_delete_form.html.twig') }}
{% endblock %}
